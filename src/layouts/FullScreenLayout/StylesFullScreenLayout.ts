export const StylesFullScreenLAyout = () => {
  const container = {
    minHeight: '100vh',
  };

  const render = {
    pl: { xs: '10px', md: '30px' },
    pr: { xs: '10px', md: '30px' },
  };

  return { container, render };
};
